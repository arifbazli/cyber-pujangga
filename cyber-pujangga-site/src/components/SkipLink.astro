---
/**
 * Skip link — 100% mockup-faithful (`.skip` class).
 */
import { type Locale, t } from "../i18n";

interface Props {
  lang: Locale;
}

const { lang } = Astro.props;
const strings = t(lang);
---

<a href="#main" class="skip">{strings.theme.skipLabel}</a>